# 2026-05-19

## Meta ads 2-day operating check

- Ran `meta_doctor.py`; Meta access is healthy for ad account `act_286631686227626`, Page, Instagram, and pixels.
- Audited `Remarketing USA` (`120234146733660563`) and `Remarketing USA - Free Trial Test` (`120242845828470563`). Both campaigns are still `PAUSED` / `effective_status=PAUSED`, updated 2026-04-24, so there was zero delivery in the last 2 days and last 7 days.
- Ad sets are configured `ACTIVE` but blocked by campaign pause. Active ads are campaign-paused; broken old control ads `120234146733690563` and `120234146733680563` remain paused with hard Meta processing errors. Test carousel `120242846212370563` remains paused.
- Since test launch (`2026-03-25` through `2026-05-19`): control spent ZAR 11,357.27 for 274 clicks, 0.45% CTR, 69 landing page views, ZAR 164.60/LPV. Test spent ZAR 8,665.64 for 103 clicks, 0.94% CTR, 43 landing page views, ZAR 201.53/LPV.
- Custom conversions confirmed: `554671110825108` = Free Trial Signup, `1145482970670900` = Free Trial Started, `936973485167575` = Website Visitor. The only visible custom conversion in campaign insights was Website Visitor; no visible Free Trial Signup / Customer Signs Up signal appeared in the audit window.
- No automatic changes made: the account is paused rather than accidentally misconfigured, and the standing caution is not to restart Meta spend silently. Current read: control has lower LPV CPA since launch; test has stronger CTR but not enough fresh delivery or signup signal to justify budget expansion.

## Platform direction: stay in OpenClaw, stop Hermes migration

- Chris decided to forget the Hermes migration for now because it feels too complex.
- My recommendation aligns: stay in OpenClaw and focus on making Nicole's current setup excellent instead of paying migration tax.
- Next setup focus should be reliability and polish: voice replies, memory hygiene, skills/tooling, heartbeat checks, backups/git sync, and marketing-ops workflows.

## OpenClaw doctor cleanup

- Ran `openclaw status`, `openclaw doctor --deep --non-interactive`, and `openclaw security audit --deep` after Chris asked to fix setup issues.
- Safe fixes applied: set `/data/.openclaw/openclaw.json` permissions to `600`; archived orphan session/transcript files by renaming them to `.deleted.<timestamp>`; verified doctor no longer reports config-file readability or orphan transcript warnings.
- Remaining items need deliberate approval rather than silent changes: command owner configuration for Chris, plugin bundled discovery allowlist mode, exec security posture, Slack/Telegram group policy choices, gateway service PATH/install cleanup, OpenClaw update, and MEMORY.md bootstrap compaction.
- Security audit flagged local Google Analytics / Search Console scripts for `child_process` token-helper usage; reviewed the flagged files and they call fixed local token helpers, not user-supplied shell commands. Treat as reviewed/trusted but worth refactoring later if we want a clean audit.

## Command owner configured

- Chris approved setting the OpenClaw command owner to his Telegram account.
- Configured `commands.ownerAllowFrom` as `telegram:5810523015` in `openclaw.json`.
- Verified via config inspection and `openclaw doctor --deep --non-interactive`; the prior “No command owner is configured” doctor warning is gone.

## AlphaClaw/OpenClaw doctor review from screenshot

- Chris sent an AlphaClaw doctor screenshot and asked to carefully review/fix what is needed.
- Re-verified against live `openclaw doctor --deep --non-interactive`, `openclaw status --deep`, and security audit output rather than trusting stale UI rows.
- Fixed plugin allowlist hardening by changing `plugins.bundledDiscovery` from `compat` to `allowlist`; this removed the bundled provider discovery warning.
- Fixed MEMORY.md bootstrap truncation by setting `agents.defaults.bootstrapMaxChars` to `30000`; total bootstrap remains under the configured total cap and doctor no longer reports bootstrap truncation.
- Restarted the gateway via the gateway tool after config changes and verified it was reachable; Telegram and Slack both reported OK.
- Remaining live doctor warnings were deliberately not auto-fixed: Telegram/Slack group allowlist policy needs an intentional access decision; gateway service PATH/launcher cleanup affects the running service; exec security full is a tradeoff between safety and operator speed; OpenClaw update requires explicit update approval.
