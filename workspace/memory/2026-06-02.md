# 2026-06-02

## OpenClaw housekeeping: skill runtime drift and memory sidecars
- Fixed `skills/github-vercel-builder/SKILL.md` so substantial builds use the currently available `sessions_spawn` runtime `subagent` instead of unsupported `runtime: "acp"`, while preserving Codex model intent, verification, GitHub publishing, and Vercel deployment workflow.
- Moved transcript-style session sidecars out of active `memory/` root into `memory/session-sidecars/active-root-cleanup-2026-06-02/` so active memory is reserved for canonical daily notes and curated memory.
- Corrected stale archived `$59/month` pricing guidance in the moved 2026-05-27 sidecar; current durable pricing direction remains 5 clients at `$49/month` up to 1,000 clients at `$4,900/month`.

## OpenClaw housekeeping: privacy, plugin, and persistence cleanup
- Removed tracked raw `migration.log` and `migration_full.log` because they contained customer email-level migration output; raw files were archived privately under `/data/.openclaw/private-archives/migration-logs-2026-06-02/` and replaced with `migration-logs-sanitized-summary.md`.
- Added gitignore rules for raw migration logs, generated dream output, transient image preview files, and raw help-center screenshot captures.
- Removed stale external `slack` plugin allowlist drift from `openclaw.json`; local Slack skill visibility remains available and `openclaw skills check` no longer reports the missing-plugin warning.
- Updated the weekly Oviond cron payload from unsupported high-thinking Codex settings to the supported medium-thinking setting so the Monday report job should stop failing with request-schema errors.
- Updated `HEARTBEAT.md` so ad monitoring follows Chris's standing ads-off hold first: any Google Ads or Meta/Facebook spend/delivery before website + tracking + explicit reauthorization is alert-worthy.
- Corrected stale AlphaClaw bootstrap guidance that said no Google accounts were configured; Google Workspace is operational through the live `gog` delegated-impersonation path.
- Replaced stale `sag`/ElevenLabs voice-storytelling guidance in `AGENTS.md` with provider-neutral guidance that points agents to the configured TTS path plus `TOOLS.md` / `MEMORY.md`.
- Curated `MEMORY.md` from 24,066 bytes to about 12.4 KB so it fits under OpenClaw's per-file injection cap without silently dropping the middle of durable context.
- Added working Google Tag Manager helper scripts (`gtm_auth_doctor.js`, `gtm_request.js`, `gtm_discover.js`, `gtm_audit.js`) and updated GTM auth docs to match the real Google Workspace service-account impersonation path.
