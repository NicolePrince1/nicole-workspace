# 2026-03-10

## Role and skill visibility

- Chris asked for a summary of Nicole’s role and available skills.
- A mismatch showed up between the actual installed skill set and some improvised answers about skills; this highlighted the need to rely on the real skill inventory rather than paraphrasing from memory.
- Chris explicitly asked that the `skill-creator` skill be used for skill-related requests.

## Landing page / coding-agent episode

- Chris asked for a super simple red hello-world landing page and a live link.
- The initial claim that a coding agent was already building it was premature; Codex CLI was not actually available in this runtime.
- The page was then built and deployed through a direct GitHub + Vercel workflow instead.
- Early deployment attempts were hindered by auth / SSO issues, which were then corrected so the final public link worked.
- This episode is a reminder to verify tool/runtime availability before promising that a delegated coding path is already running.

## Workspace hygiene and operating fixes

- Workspace onboarding was confirmed complete, and the active `BOOTSTRAP.md` was removed from the normal startup path.
- A stale AlphaClaw-managed bootstrap note incorrectly claimed no Google accounts were configured; the live Google Workspace setup was verified and a corrective note was added to `TOOLS.md` instead of editing the managed source directly.
- `MEMORY.md` was audited for secret-like material, scrubbed, and then refactored to hold only durable business context and key decisions.
- Local operator details such as helper paths, wrapper commands, and implementation notes were moved into `TOOLS.md`.

## Memory-management decision

- The workspace should maintain one canonical daily note at `memory/YYYY-MM-DD.md` for each date.
- Same-day sidecar notes should be merged into that canonical daily note, then removed from the active memory path.
- `AGENTS.md` startup guidance should stay aligned with that single-file daily-note convention.

## Google Ads setup direction

- For reliable, programmatic Google Ads access on Oviond’s own account, the recommended path is an internal-use Google Ads API setup tied to the MCC, with Standard Access pursued instead of relying on Basic Access for long-term operational work.
- The intended positioning for Google’s review should be internal-only usage for Oviond’s own ad performance analysis, reporting, account management, and campaign optimization.
- A likely application risk noted at the time was any mismatch between the company’s real principal place of business and what was entered in the Google Ads API Center.

## Skill-testing / tooling lessons

- Registry-level or metadata-level skill checks are not the same thing as end-to-end functional proof; future skill validation should distinguish clearly between those levels.
- Some live skill claims on 2026-03-10 were overconfident before end-to-end verification was complete, especially around Stripe connectivity and broad skill smoke-test coverage.
- The Google Workspace wrapper itself was live, but command syntax needed to be learned precisely rather than improvised.
