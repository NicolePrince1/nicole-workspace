# 2026-03-26

## Google Ads access status

- Re-tested the Google Ads skill live. The local skill wiring works far enough to mint a service-account token, but live Google Ads API calls still fail with `PROJECT_DISABLED` for Cloud project `480335022137`.
- Latest live request IDs from the 2026-03-26 retest:
  - `3rfj0N0ZAVrS3TFw875Q8w`
  - `OVTK4LAy0MYaDWQUco7oaQ`
  - `Z6N9HfaBM2mIAxjfNRCyJw`
- Current posture remains: service-account-first is still the right architecture, but Google-side project/API access is still blocked.

## Google Ads fallback architecture plan

- Chris is open to starting clean if needed.
- Agreed direction: do not restart blind; use the old Google Ads account as a forensic / historical data source, then build a clean execution environment and operator system from verified performance data.
- Proposed structure:
  1. historical intelligence layer from the old account
  2. clean execution environment for future API-controlled operations
  3. Nicole operator layer for reporting, decisions, and safe execution
- Strong recommendation: reset the execution stack, not the learning.
- Verified-data rule was reinforced again: recommendations should be grounded in real account data, not speculative reconstructions.

## Browser-led recovery path

- Chris asked to use browser access to pull what is needed from the old account instead of relying only on manual exports.
- Browser automation was attempted, but browser control was unavailable even though the gateway itself was reachable.
- Next resume step:
  - Chris opens/logs into a Chromium browser with Google Ads access
  - restart the OpenClaw gateway if needed
  - then resume browser-led extraction of campaigns, search terms, keywords, negatives, devices, geos, conversion actions, landing pages, and change history.

## Resume note

- Chris explicitly asked that this progress carry forward and be resumed later.
