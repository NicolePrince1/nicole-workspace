# 2026-04-03

## Google Ads recovery succeeded via fresh GCP project

- Re-audited the Google Ads skill and confirmed the old blocker was tied to the old Cloud project `oviond-workspace-cli` / `480335022137`, not the overall service-account-first architecture.
- Chris created a fresh GCP project: `northern-card-492208-q6`.
- In that project:
  - Google Ads API was enabled.
  - service account `nicole-google-ads@northern-card-492208-q6.iam.gserviceaccount.com` was created.
  - the JSON key was added to AlphaClaw Envars as `GOOGLE_ADS_SERVICE_ACCOUNT_JSON_CONTENT`.
  - the service account was added to Google Ads MCC `6387956297`.
- Live retest then succeeded for:
  - access-token mint
  - `customers:listAccessibleCustomers`
  - target-account query for client `2906154258`
  - manager→client linkage query
- Practical conclusion: Google Ads production API access is operational again.

## Google Ads live account read and immediate operating stance

- Verified the apparent free-trial conversion action:
  - `Trial_Signup_Completed_Form`
  - category `SUBMIT_LEAD_FORM`
  - enabled, primary, and included in conversions
- PMAX campaign `PMAX- Start Trial USA` (`21330916203`) was the only currently proven acquisition engine.
- Display campaign `Display Remarketing USA Feb 2026` (`23607074353`) was identified as a waste-risk:
  - heavy low-quality placement mix
  - meaningful spend with no recent true trial-signup conversions
  - polluted reporting due to low-value conversion activity like `page_view`
- Chris wants Google Ads optimized around real free-trial signups with a practical ceiling around `R1000/day` total and low tolerance for waste.
- Immediate operating stance:
  - keep PMAX live
  - pause or near-zero bad Display activity
  - do not force major bidding changes before asset quality is cleaned up

## Historical PMAX audit and USA adaptation

- Chris asked for a comparison between the current PMAX and the highly successful PMAX period from roughly Feb–May last year.
- Strongest historical performance signal came from `StartTrial - South Africa` (`22441221025`), which produced very cheap trial-signup CPAs, but Chris clarified South African customers have poor stickiness / LTV and should **not** be re-scaled.
- Durable lesson from the comparison:
  - keep the core PMAX architecture
  - adapt the winning direct-response / free-trial messaging pattern into USA
  - rebuild weak image and asset quality first, especially square images
  - do not let the market-quality lesson be overridden by cheap CPA alone

## USA PMAX asset refresh + Search launch

- Chris approved a surgical refresh of USA PMAX without changing budget, bidding mode, or geo focus.
- Applied a direct-response asset refresh to asset group `6501065328`, including stronger headlines and long headlines such as:
  - `Simple Agency Reporting`
  - `Try Oviond Free Today`
  - `White Label for Agencies`
- Removed softer / weaker assets and low-value square images.
- Chris then approved adding a precision-intent USA Search campaign at `R250/day`.
- Launched campaign `USA | Search | Trial | Core Intent | 2026-04-03` (`23720451302`) with:
  - USA-only targeting
  - Search only (no Display / Search Partners)
  - campaign-level focus on `SUBMIT_LEAD_FORM`
  - tightly scoped exact + phrase keywords
  - ad groups for agency reporting software, white-label reporting, AgencyAnalytics alternative, and Swydo alternative
  - an explicit junk-intent negative-keyword layer
- Practical interpretation: Search is now live as a controlled buyer-intent expansion path, not a broad traffic play.

## Google Workspace live test succeeded

- Chris explicitly asked to test the Google Workspace skill by sending a real email and meeting invite.
- Executed successfully:
  - sent a test email to `chris@oviond.com`
  - sent a 15-minute calendar invite for `10:30–10:45 SAST`
- Practical conclusion: Google Workspace access is live and usable for direct email/calendar actions.

## Stripe skill architecture fix landed

- Chris corrected the live business truth: real MRR is about `$28,712`.
- The old Stripe skill was undercounting MRR because it relied too heavily on subscription price math.
- The skill was rewritten so canonical MRR uses invoice-backed recurring line items first, with improved fallbacks and local caching.
- Validation after the rewrite brought the live MRR output back into the expected band near Stripe dashboard truth instead of the broken roughly `$17k` estimate.
- Durable rule remains: trust the Stripe dashboard over the script whenever there is uncertainty, but the skill is now materially better for operational reads.

## Meta ads interim cutover while pixel tracking is unreliable

- Chris approved an immediate interim Meta simplification while conversion tracking remains unreliable until next week.
- Live cutover executed successfully:
  - kept only campaign `Remarketing USA` (`120234146733660563`) live
  - left budget at `R350/day` rather than forcing the full `R500/day`
  - switched ad set `United States` (`120234146733780563`) to `LANDING_PAGE_VIEWS`
  - restricted placements to Facebook `feed` + `right_hand_column` on mobile + desktop
  - kept only ad `Images - Product` (`120234146733690563`) active
  - paused the full lead-optimized test campaign `Remarketing USA - Free Trial Test` (`120242845828470563`) and both of its ads
- Practical operating rule until pixel work is fixed:
  - judge Meta by LPV cost and backend/app signup reality, not Meta-reported leads or current custom-conversion counts

## Meta ad-object break/fix after the cutover

- Chris reported that the surviving Meta campaign looked broken.
- Live diagnosis showed the campaign and ad set were still active; the real problem was the live ad object itself.
- Root cause:
  - intended live ad `Images - Product` (`120234146733690563`) was `PAUSED` with `WITH_ISSUES`
  - Meta surfaced `error_code 2643069` / `Ad processing error`
- Recovery action:
  - created replacement ad `Images - Product (recovery 2026-04-03)` with id `120243374907550563`
- Final verification:
  - campaign remained `ACTIVE`
  - ad set remained `ACTIVE`
  - recovery ad cleared review and became fully `ACTIVE`
- Practical takeaway: the break was caused by a corrupted ad object, not by the campaign/ad-set configuration.

## Agent/skill summary correction and managed-doc escalation

- Chris asked for a setup/skills explanation and later pushed for a correction on the `alphaclaw` skill.
- Durable interpretation confirmed:
  - `alphaclaw` is a narrow routing skill for setup/config tasks in the AlphaClaw UI
  - it is not a broad capability or execution mode
- Chris also asked for an escalation to AlphaClaw so the managed bootstrap guidance no longer contradicts the current Google Workspace connected state and no longer refers to `gog-cli` instead of `gog`.
- Constraint reaffirmed: do not manually edit the locked bootstrap files; corrections to managed bootstrap guidance must be escalated upstream.
