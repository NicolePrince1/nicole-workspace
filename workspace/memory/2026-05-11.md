# 2026-05-11

## OpenAI realtime voice update review

- Chris asked for a full review of OpenAI's May 7 voice-model update and how OpenClaw can use it for Nicole's voice workflow.
- OpenAI announced three Realtime API models: `gpt-realtime-2` for reasoning voice agents, `gpt-realtime-translate` for live translation, and `gpt-realtime-whisper` for streaming transcription.
- Current OpenClaw runtime inspected on this host: OpenClaw `2026.5.6`; TTS is enabled with `auto: inbound`, provider `openai`, model `gpt-4o-mini-tts`, voice `coral`, speed `1.25`; `tools.media.audio` is not explicitly configured.
- OpenClaw natively supports: outbound TTS via `messages.tts`; inbound batch audio transcription via `tools.media.audio`; Telegram native voice-note delivery; Control UI Talk via realtime voice; and Voice Call realtime voice/streaming transcription via plugin config.
- Current OpenClaw OpenAI realtime defaults in docs/source are still `gpt-realtime-1.5` for realtime voice and `gpt-4o-transcribe` / `whisper-1` paths for transcription, so adopting `gpt-realtime-2` or the new realtime transcription/translation models likely needs config override and live smoke testing, not blind assumption.

## Talk mode docs review

- Chris asked to review `https://docs.openclaw.ai/nodes/talk` after the OpenAI voice update.
- Current live docs add a more complete Talk architecture than the local installed docs: native Talk, browser realtime Talk, Gateway relay, transcription-only Talk, `talk.client.create`, `talk.session.create`, `talk.client.toolCall`, and `talk.realtime` config.
- Important difference: local installed OpenClaw docs/source on this host appear older than the live docs for Talk; local `/app/node_modules/openclaw/docs/nodes/talk.md` does not yet show the new `talk.realtime` section, and grep did not find `gpt-realtime-2` / new Talk realtime terms in the installed dist. Treat the live docs as the target direction, but verify runtime support after updating OpenClaw before applying config.
- New preferred plan: keep Telegram voice-note TTS as stable batch voice, then upgrade OpenClaw/runtime and configure Talk realtime separately with OpenAI `gpt-realtime-2`, `coral` or a higher-quality realtime voice (`cedar`/`marin`) after smoke testing.
## Meta ads 2-day operating check

- Ran the scheduled Meta doctor successfully: ad account, campaign listing, Page, Instagram, and pixel access all worked.
- Audited `Remarketing USA` (`120234146733660563`) and `Remarketing USA - Free Trial Test` (`120242845828470563`) in `act_286631686227626`.
- Both campaigns were `PAUSED` / `effective_status=PAUSED`, so there was zero delivery in the last 2 days and last 7 days. Their ad sets are configured `ACTIVE` but blocked by campaign pause.
- Broken control ads `120234146733690563` and `120234146733680563` remain paused with Meta hard processing errors; the recovery image ad is configured active but campaign-paused. Test image ad is configured active but campaign-paused; test carousel is paused.
- Since the test launch on 2026-03-25: control spent ZAR 11,357.27 for 274 clicks, 0.45% CTR, 69 LPVs, ZAR 164.60/LPV; test spent ZAR 8,665.64 for 103 clicks, 0.94% CTR, 43 LPVs, ZAR 201.53/LPV.
- Only visible custom conversion in reporting was `offsite_conversion.custom.936973485167575`, which maps to Website Visitor, not signup. Free Trial Signup / Customer Signs Up custom conversions exist but showed no visible signal in this audit.
- No automatic changes were made because restarting spend while both campaigns are paused is not a safe silent adjustment under the standing ads-off/tracking caution; Chris should explicitly confirm if these should resume.
