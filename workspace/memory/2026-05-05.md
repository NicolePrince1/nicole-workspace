# 2026-05-05

## Voice response audit

- Chris called out that voice-note replies must include both text and voice. This is already a standing preference in `MEMORY.md` and should be treated as a bug whenever missed.
- Live TTS status showed `messages.tts.provider: openai` configured, OpenAI provider available, selected, and usable.
- Live TTS status also showed auto-TTS disabled: `enabled: false`, `auto: off`. That explains why voice replies depended on me explicitly calling the `tts` tool instead of automatically attaching audio after inbound voice notes.
- Current configured voice settings are OpenAI `coral` at speed `1.25`, matching Chris's preference. OpenClaw docs show OpenAI defaults to `gpt-4o-mini-tts`, with `coral` as a supported voice and Opus preferred for Telegram voice-note replies.
- Proposed safest fix: set `messages.tts.auto` to `inbound`, explicitly set OpenAI model to `gpt-4o-mini-tts`, keep `voice: coral`, keep `speed: 1.25`, and optionally add concise style instructions for Nicole's spoken voice. This needs Chris approval because it changes OpenClaw config.

## Voice response fix applied

- Chris approved the config change.
- Updated `/data/.openclaw/openclaw.json` so `messages.tts.auto` is `inbound`, provider is `openai`, model is pinned to `gpt-4o-mini-tts`, voice stays `coral`, speed stays `1.25`, and OpenAI TTS instructions describe Nicole's spoken style.
- Ran `openclaw gateway restart`; live `openclaw infer tts status --json` initially still reported old local preference state, then `openclaw infer tts enable --gateway` created `/data/.openclaw/settings/tts.json` and I corrected that ignored persistent local preference to `auto: inbound` rather than `always`.
- Verification after the fix: `openclaw infer tts status --json` reports `enabled: true`, `auto: inbound`, provider `openai`, configured `true`.

## Meta ads 2-day operating check

- Ran the scheduled Meta Ads operating check for account `act_286631686227626`; `meta_doctor.py` passed.
- Both audited campaigns are still intentionally `PAUSED` after Chris's 2026-04-24 paid-ads hold: `Remarketing USA` (`120234146733660563`) and `Remarketing USA - Free Trial Test` (`120242845828470563`). No reactivation was made because Chris's durable instruction is not to turn Meta/Facebook ads back on until the new site and tracking are ready.
- Last 2 days and last 7 days show zero delivery for both campaigns.
- Since the test launch (`2026-03-25` through `2026-05-04`): control spent `R11,357.27` for `69` LPVs (`R164.60/LPV`) and `399` visible `Customer Signs Up` custom-conversion signals (`R28.46` each); test spent `R8,665.64` for `43` LPVs (`R201.53/LPV`) and `352` visible custom-conversion signals (`R24.62` each).
- Current read: no budget expansion while the paid-ads hold is in force; test is not ready to scale because it is paused and tracking/site migration is still the gating issue.
