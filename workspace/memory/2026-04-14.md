# 2026-04-14

## Nicole local-time preference was corrected

- Chris explicitly said Nicole should treat herself as living with him in South Africa, specifically Cape Town.
- Practical rule: default to Cape Town / SAST for casual time references and shared-local-context phrasing unless a task explicitly needs another timezone.

## Google Ads CPA was benchmarked against last year

- Chris asked how current CPA compares to the same period last year and to the stronger April-to-June run from 2025.
- Nicole used completed days only and anchored the comparison on the same primary trial goal, `Trial_Signup_Completed_Form`, to avoid polluted top-line conversion noise.
- Resulting comparison:
  - current window (`2026-04-01` to `2026-04-13`): `R24,427.48` spend, `147` trial signups, `R166.17` CPA
  - same dates last year (`2025-04-01` to `2025-04-13`): `R3,074.57` spend, `53` trial signups, `R58.01` CPA
  - April to June 2025 benchmark: `R122,465.14` spend, `1,237` trial signups, `R99.00` CPA
- Blunt read: current CPA is about `2.9x` worse than the same April slice last year and about `68%` worse than the broader successful April-to-June 2025 benchmark.

## Google Ads follow-up locked in

- Chris chose to give the cleaned-up US Google Ads setup one full week before making the next call.
- The immediate intent is to judge `PMAX- Start Trial USA` after the 2026-04-14 cleanup, with `Oviond Website + App (web) page_view` demoted from primary-goal status and `USA | Search | Trial | Core Intent | 2026-04-03` paused.
- Nicole scheduled a one-shot cron review for `2026-04-21 09:00 SAST` (`Google Ads PMAX USA post-cleanup 7-day review`) to audit the last 7 complete days, write the outcome into `memory/2026-04-21.md`, and send Chris a blunt verdict on whether PMAX USA improved once the noise was stripped out.

## Heartbeat revenue pulse flagged a real drop

- A 2026-04-14 heartbeat revenue pulse showed the last 7 days of paid Stripe invoices at `R4,636.29` versus `R6,455.46` in the prior 7 days, a drop of `R1,819.17`.
- `past_due` was still `38`, well above the >20 alert threshold.
- Practical read: this was a real attention-needed revenue and collections warning, not a no-issues heartbeat.

## Sequenzy live audit and draft demo sequence were confirmed

- Chris provided a live `SEQUENZY_API_KEY`, and Nicole verified read-only access against both the documented Sequenzy REST API and broader live account surfaces.
- Current Sequenzy snapshot for Oviond: one verified website (`oviond.sequenzymail.com`), two lists, about `6,265` subscribers, two draft sequences (`Product Onboarding`, `Past Due Recovery`), one draft campaign, one enabled transactional email, and zero send/open/click activity so far.
- The live account also exposed a meaningful template library, about `17` templates, including product-onboarding and past-due recovery work plus a couple of sloppy placeholder drafts.
- Nicole created a safe draft-only demo sequence named `Nicole Demo - Trial Activation Lab (Draft Only)` with Sequenzy id `0eade40c7d844da1a3a287bf`.
- The demo uses an `event_received` trigger (`oviond.demo.trial_started`) so it should not accidentally enroll real contacts during testing.
- The demo proved draft sequence creation, HTML-to-block conversion, and draft updating via API. The built draft spans `5` emails across `10` days with `11` nodes total, and branch-writing via API is still not yet validated.
- Chris explicitly wants the demo sequence left in Sequenzy for now and removed later.

## Paid media was shut off pending conversion-tracking cleanup

- Chris directed Nicole to pause all Google Ads and Meta paid media until conversion tracking is fixed.
- Google Ads live write result: all enabled campaigns were paused successfully, including `PMAX- Start Trial USA` and `StartTrial  - South Africa`.
- Meta live write result: all three active campaigns were paused successfully, including `Remarketing USA`, `Remarketing USA - Free Trial Test`, and `Remarketing South Africa - Temp`.
- Verification after the writes showed zero enabled Google Ads campaigns and Meta campaign status returning `PAUSED` on the active set.
- Later on 2026-04-14 Chris explicitly tested whether Nicole had deleted Google Ads campaigns instead of pausing them. The deeper audit showed the campaigns still existed and were `PAUSED`; the scary `Rows: 0` check was just an enabled-campaign filter, not proof of deletion.
- Practical operating rule: keep paid media off until tracking events are corrected and explicitly re-approved for relaunch.

## Voice-note reply behavior was restored, and the failure path was isolated

- Chris explicitly re-stated on 2026-04-14 that whenever he sends a voice note, Nicole must reply with both a text response and a voice-note response.
- Nicole verified that the premium OpenAI TTS configuration still exists in `/data/.openclaw/openclaw.json`, including provider `openai`, voice `coral`, and speed `1.25`.
- Early debugging showed the premium voice stack still existed and the remaining failure was in the reply-media attachment or routing path after TTS generation.
- Later live forensics narrowed the stronger root-cause candidate to the reply-suppression path in `/app/node_modules/openclaw/dist/reply-payloads-dedupe-OtM2z7YX.js` and `/app/node_modules/openclaw/dist/agent-runner.runtime-CUJd1OG7.js`, where `message` -> `tts` -> `NO_REPLY` could suppress the queued voice-note payload.
- The main voice-note reply path was then proven working again via explicit Telegram voice-note sends. Chris confirmed the test reply worked and the full text-plus-voice behavior was back.
- Practical rule: treat any future miss as a delivery-path bug, not a preference change or a lost TTS capability.

## Google Tag Manager access became live and the hostname cleanup shipped

- Chris asked Nicole to build a proper GTM skill so GTM could be audited and managed directly rather than guessed through GA4 symptoms.
- Nicole created the local skill skeleton at `skills/google-tag-manager/` and documented the first-pass operator flow.
- Chris accepted Nicole's GTM invitation, confirmed the Tag Manager API was enabled, and added GTM scopes to the existing Workspace service-account domain-wide delegation client.
- Live retest succeeded through the service-account impersonation path. Nicole could list GTM accounts and containers via the Tag Manager API.
- Confirmed GTM account: `Oviond` (`accounts/6002040978`).
- Confirmed containers:
  - mixed web container `www.oviond.com and v2.oviond.com` (`GTM-WXRJGTK`, `containers/32460546`)
  - server container `www.oviond.com - Server` (`GTM-WGXKZQH`, `containers/65118591`)
- Nicole later confirmed true GTM write/publish authority for `nicole@oviond.com`, created workspace `accounts/6002040978/containers/32460546/workspaces/109`, added trigger `Init - www.oviond.com only` (`triggers/97`), repointed GA4 website tag `Google Tag G-9WY3NZJFJK` (`tags/96`) to that trigger, and published version `accounts/6002040978/containers/32460546/versions/109`.
- Nicole also restricted the live `Unami Analytics` tag to `www.oviond.com`, materially reducing website analytics pollution from `v2.oviond.com`.
- Durable read: the website-property pollution issue was materially fixed at the tag-firing layer. Remaining `v2` tags are mainly remarketing/conversion plumbing and should be audited separately.

## The 10-day operating push and durable tracker were established

- Chris said he had secured an additional `$70,000` for paid media, but explicitly did not want any of it spent until measurement and the operating system were tightened up.
- Nicole’s blunt operating objective for the next 10 days became: build a clean, measurable, activatable funnel, then relaunch paid media with discipline.
- The practical priority order was set as: measurement truth, funnel truth, activation, retention, lifecycle, product-friction removal, then controlled paid relaunch.
- Nicole created the durable tracker at `projects/oviond-growth-operating-system.md` as the single living source of truth for current state, top 10 priorities, next actions, and review rhythm.
- Chris was told to manage it conversationally with commands like `show me the list`, `mark #3 done`, `move #5 to NOW`, and `add this as a new task`.

## Full business audit stayed blunt: better visibility, but not a clean scale phase

- Chris asked for a full check on where things stood, the biggest wins, the biggest problems, and the next priorities.
- Nicole’s durable read was that Oviond had materially better visibility and cleaner platform access than a few days earlier, but was still constrained mainly by retention, activation, and trustworthy acquisition data rather than by a lack of ideas.
- Biggest wins in that audit: GTM access plus live hostname cleanup, paid-media shutdown pending tracking cleanup, Sequenzy becoming operational enough to build on, and a sharper cross-stack read via Stripe and Gleap.
- Biggest problems in that audit: bad Google Ads efficiency versus last year, a soft revenue pulse, `past_due = 38`, retention still being the strategic wound, and lifecycle still not being live enough.
- Durable next-task stack from that audit: finish tracking cleanup and truth validation, turn Sequenzy into a live operating system, attack retention directly, use Gleap as a product-priority feed, and rebuild the weekly operating scoreboard.

## AlphaClaw audit found a mostly clean harness, with remaining issues sitting at the OpenClaw layer

- Chris asked for a strict AlphaClaw audit against AlphaClaw docs only, with no guessing and no undocumented fixes.
- Nicole’s blunt read was that the AlphaClaw harness was mostly correctly configured, not a total mess, but not perfectly clean either.
- Clean items confirmed: AlphaClaw running as the main Railway process, OpenClaw gateway reachable through the AlphaClaw-managed local path, required GitHub and channel tokens present, AlphaClaw’s git auth shim installed at `/usr/local/bin/git`, managed hourly Git sync present, and documented env vars like `OPENCLAW_GATEWAY_TOKEN`, `PORT`, and `ALPHACLAW_ROOT_DIR` visible in the live process env.
- Real issue found: the hourly sync script had been brittle because it sourced `/data/.env` under bash even though that file held JSON secrets that are not shell-safe in this Railway setup.
- Doc-backed fixes completed: repaired the managed hourly Git sync script, cleaned obvious generated junk out of Git tracking, verified GitHub push worked again, and verified AlphaClaw’s documented `alphaclaw git-sync` path loaded `.env` cleanly and returned `No changes to commit`.
- Nicole deliberately did not change `openclaw.json` permissions, `exec security=full`, or broken app-level cron jobs because those were not clearly AlphaClaw-doc-backed fixes.
- Durable interpretation: AlphaClaw itself was mostly sound after the doc-backed cleanup; the remaining meaningful cleanup lived mostly in OpenClaw app/security/config hygiene rather than in the AlphaClaw harness.
